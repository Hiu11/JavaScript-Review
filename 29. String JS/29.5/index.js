// replace(); repeat();

// let newString = replace(oldValue, newValue); 
// thay thế giá trị old đầu tiên dc tìm thấy trong chuỗi bằng new

// string.repeat(count)
// lặp chuỗi count lần, cout là số lần nhập chuỗi


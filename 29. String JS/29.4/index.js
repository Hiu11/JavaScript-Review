// concat(), toUpperCase(), toLowerCase(), charAt()

// let newString = str1.concat(str1,str2,...,strN); nối các chuỗi thành str1
let ten = "hieu";
let ho = "do"
let hoten = ho.concat("", ten);
console.log(hoten);

// toUpper.. chuyển toàn bộ chuỗi sang in hoa
// toLower.. chuyển toàn bộ chuỗi sang in thường
// charAt(index) trả về chuỗi nằm ở vị trí index dc truyền vào
